# NebulaX V1 性能监测报告

**日期：** 2026-05-17 | **系统：** Ubuntu 22.04 | **网络：** 127.0.0.1 loopback TCP

## 系统配置

| 组件 | 规格 |
|------|------|
| CPU | 12th Gen Intel Core i9-12900HX（Alder Lake，8 P-core + 16 E-core） |
| 最大频率 | 4900 MHz |
| L1d cache | 48K per P-core |
| L2 cache | 1.3M per core，14M total |
| L3 cache | 30M |
| 内存 | 31 GB |
| 内核 | Ubuntu 22.04.5 LTS，x86_64 |
| 绑核 | 服务端 core 6（P-core），客户端 core 5（P-core） |

---

## 测试方法

V1 采用两种互相补充的 benchmark 模式：

**Ping-pong（RTT 模式）：** 客户端发一条命令，等回复，再发下一条。测量的是**单笔订单的交互延迟**——交易者下单一笔，多久能知道结果（成功/失败/成交）。能直接反映 P50/P99/P999 尾延迟。代价是服务端 `recv()` 频繁无数据可读，触发 `schedule()`，产生大量上下文切换。

**Pipeline（吞吐模式）：** 客户端双线程——发送线程只管发，接收线程只管收。测量的是**引擎的吞吐上限**——gateway 批量转发请求时，引擎每秒能处理多少条。代价是延迟包含排队时间（队列深度 / QPS），不代表单笔交互延迟。

两种模式都跑同一个命令序列（50% NEW + 25% CANCEL + 25% BOOK，价格区间 10000~14999 重叠），每轮 500K 条，重复 3 轮。

---

## 对比数据

### 关键指标一览

| 指标 | Ping-pong | Pipeline | 对比分析 |
|------|:---------:|:--------:|---------|
| **QPS** | 141,070 | **664,053** | 同段代码，仅测试模式不同，QPS 差 4.7 倍——瓶颈是交互模式而非处理能力 |
| **平均延迟** | 7 µs | 175ms | 两种延迟不可比：7µs 是单笔处理耗时，175ms 是 500K 笔排队平均等待 |
| **P50** | 7 µs | 195 ms | Ping-pong 延迟来自处理本身；Pipeline 延迟来自排队，P50 随队列深度线性变化 |
| **P99** | 10 µs | 253 ms | Ping-pong 的 P99 和 P50 差距小（3µs），调度抖动不大；Pipeline P99≈P999，排队均匀 |
| **P999** | 19 µs | 255 ms | Pipeline 下 P99≈P999，队列处理稳定；Ping-pong 受调度偶尔波动 |
| **ctx/s** | 121,250 | 207 | Ping-pong 每秒 12 万次切进切出，每次 ~9µs 空转；Pipeline 因数据没断，几乎不切 |
| **IPC** | 1.98 | **2.94** | Ping-pong 频繁断流、流水线清空；Pipeline 连续处理，cache 热，IPC 高 48% |
| **cycles** | 24,517M | 9,217M | 同一批命令，Ping-pong 花 12.4s 跑完，Pipeline 花 3.1s——差值主要在调度空转上 |
| **instructions** | 48,509M | 27,139M | Ping-pong 跑 12.4s 执行了更多指令，多出来的主要来自调度器和 idle 循环 |
| **cache-misses** | 2,134,784 | 3,304,867 | Pipeline 绝对 miss 多，但指令总量高，每指令 miss 率更低；Ping-pong的miss 少，但跑得慢、每次 miss 等加载的时间比例更高 |
| **branch-misses** | 24,592,041 | 12,729,005 | Ping-pong 每次调度回来预测器历史丢失，前几个分支在冷猜；Pipeline 连续跑，预测器一直预热 |

### 逐轮明细

#### Ping-pong

| 轮次 | avg | P50 | P99 | P999 | QPS |
|-----|----:|----:|----:|----:|----:|
| 1 | 7 µs | 7 µs | 10 µs | 18 µs | 140,467 |
| 2 | 7 µs | 7 µs | 10 µs | 20 µs | 140,613 |
| 3 | 7 µs | 7 µs | 10 µs | 19 µs | 142,130 |
| **平** | **7 µs** | **7 µs** | **10 µs** | **19 µs** | **141,070** |

#### Pipeline

| 轮次 | avg | P50 | P99 | P999 | QPS |
|-----|----:|----:|----:|----:|----:|
| 1 | 174 ms | 197 ms | 247 ms | 248 ms | 700,086 |
| 2 | 175 ms | 194 ms | 255 ms | 256 ms | 691,312 |
| 3 | 177 ms | 195 ms | 259 ms | 260 ms | 687,328 |
| **平** | **175 ms** | **195 ms** | **253 ms** | **255 ms** | **692,909** |

> Pipeline 的延迟主要是排队延迟。500K 条命令排成一队，平均等待时间 ≈ (500K / QPS) / 2 ≈ 0.36s。实测 175ms 偏低，因为发送线程还没发完所有命令时接收线程已开始处理。

---

## 分析

### 上下文切换差异

| 模式 | ctx/s | 原因 |
|------|:-----:|------|
| Ping-pong | 121,250 | 每次 recv 无数据 → schedule → 等调度回来 |
| Pipeline | 207 | recv 一直有数据，不需要让出 CPU |

差值 ~121K ctx/s = 调度器每秒来回切换 12 万次，每次 ~9µs 白费。这部分在 Pipeline 下降到 207 ctx/s，因为数据连续。

### IPC 对比

| 模式 | IPC | 对比分析 |
|------|:---:|---------|
| Ping-pong | 1.98 | 每 cycle 干 2 条指令，调度频繁打断流水线 |
| Pipeline | 2.94 | 连续执行，指令和数据 cache 热点保持，流水线填满 |

2.94 对 C++ 应用来说偏高，说明热点数据基本在 L1/L2 cache 中命中，没怎么走 LLC。

### 内核安全检查

| 模式 | kernel 安全开销 | 对比分析 |
|------|:--------------:|---------|
| Ping-pong | ~26% | 调度开销占比大，安全检查比例被相对放大 |
| Pipeline | ~10% | 业务逻辑占比更高，安全检查被稀释 |

这是 Ubuntu 默认内核 `CONFIG_HARDENED_USERCOPY` 的开销。每次 send/recv 检查用户态 buffer 越界。两模式下绝对开销相近，占比差异来自分母不同。

### syscall 不对称

Pipeline 下 `perf stat` 显示 sendto = 1,500,600（精确匹配 500K × 3 轮），recvfrom = 5,737。sendto 是 recvfrom 的 260 倍。原因是 recv 线程每次 `::recv(buf, 65536)` 一次收大量回复，但服务端每条回复都单独调一次 `send()`。每次 sendto 触发一次 `__check_object_size`。

优化方向：回复攒一批，buffer 满或 1ms 超时再 flush，sendto 次数可降到接近 recvfrom 的量级。

---

## 脉络

### V1 瓶颈

| 分类 | Ping-pong | Pipeline |
|------|:---------:|:--------:|
| 瓶颈位置 | 调度器（121K ctx/s） | 业务层（locale + 堆分配 + 撮合） |
| ctx/s | 121,250 | 207 |
| 业务逻辑占比 | ~8%（被调度稀释） | ~15% |
| 内核安全开销 | ~26%（比例放大） | ~10% |

### 硬件基线已建档

| 指标 | Ping-pong | Pipeline |
|------|:---------:|:--------:|
| QPS | 141,070 | **664,053** |
| P50 | 7 µs | 192 ms |
| P999 | 19 µs | 270 ms |
| ctx/s | 121,250 | 261 |
| IPC | 1.98 | **2.85** |
| syscall/s | ~250K | ~250K（sendto）|

### 火焰图瓶颈分解

两份火焰图：`profiling/nebulaX_flame_pingpong.svg` 和 `profiling/nebulaX_flame_pipeline.svg`。两者反映了不同的瓶颈特征。

两份火焰图的分类对比如下（自采样占比）：

| 分类 | Ping-pong | Pipeline | 差异说明 |
|------|:---------:|:--------:|---------|
| stringstream locale 初始化 | ~12% | ~15% | 两模式相近 |
| 撮合逻辑（parse/match/add/remove） | ~8% | ~15% | Pipeline 无调度稀释，占比接近翻倍 |
| 堆分配（malloc/free/new） | ~10% | ~12% | 两模式相近 |
| kernel 安全检查 | ~8% | ~10% | 两模式相近 |
| send/recv 内核 TCP 路径 | ~7% | ~10% | Pipeline 连续 IO 占比稍高 |
| 内存拷贝（memcpy） | ~8% | ~8% | 两模式一致 |
| 调度相关（spinlock/release_sock） | ~10% | ~8% | Ping-pong 多 2% 上下文切换开销 |
| 其他（析构/format/模板展开等） | ~37% | ~22% | Ping-pong 中大量 idle/cache miss/调度空闲 |

> 共性：locale + 堆分配 + memcpy 在两模式下一致，是稳态开销。
> 差异：Ping-pong 中调度和 idle 开销（~10% + ~37%）高于 Pipeline，占用了本可用于业务逻辑的 CPU。
> Pipeline 消除了调度中断，撮合逻辑占比从 ~8% 跃升至 ~15%，IPC 从 1.98 提升到 2.94。

单条命令 1.44µs 中，约 1/3 是撮合逻辑，剩下 2/3 花在语言设施和内核管理层。

---

### 发现过程与优化方向

之前用 `perf record --call-graph fp` 看热点，火焰图里 `send` 和 `recv` 加起来占了七八成，以为瓶颈在 IO。换成 `--call-graph dwarf` 后调用链能走到底了，才发现 `__libc_send` 只是一个入口，底下藏着 locale 查找、堆分配、安全检查。真正在 TCP 协议栈里的时间不到 10%。

**1. CPU 热点在 locale 与堆分配，而非网络收发**

fp 火焰图里 `__libc_send` 占比很高，容易误认为时间都消耗在发送数据上。dwarf 展开调用链后能够看到，大部分时间实际花在 `std::locale::id::_M_id` 和 `operator new`——每条命令构造 `istringstream` 与 `ostringstream` 时，触发了 locale 初始化和堆分配。两项合计接近 27%，比 TCP 协议栈开销高出一个量级。

**优化方案：** 将文本协议替换为二进制协议，收到定长 buffer 后直接按偏移读取字段。没有流构造，没有 locale 查找，也没有每命令的堆分配。理论上这 27% 的开销可以消除大部分。协议改动不依赖其他模块，可以与其他优化并行推进。

**2. 内核收发占比较小，但架构存在单连接上限**

换用 dwarf 后确认，真正的内核 TCP 收发路径占比只有 7-10%，优化空间已经有限。更紧迫的是当前架构带来的问题：

- `accept` 后 `handleClient` 不返回，第二个连接无法接入，单连接上限为 1
- Pipeline 下 send buffer 满时，`send()` 会阻塞整条线程，撮合逻辑也被迫停止
- 8 个 P-core 中仅有 1 个被使用

**优化方案：** 先引入 epoll 解除单连接限制，使一个 IO 线程通过事件通知管理多个 gateway 的收发。随后将撮合与发送拆分为独立线程：

- IO 线程负责 recv 与 epoll 事件分发
- 撮合线程独占订单簿，仅处理撮合逻辑
- 发送线程专职 send，即使阻塞也只影响自身，不波及接收和撮合

这一步的目标是提升架构的吞吐上限，而非降低单次延迟。预计执行顺序：二进制协议 → 批量 send（攒 buffer flush，sendto 从 1.5M 降到几千）→ epoll → 线程分离。

---
